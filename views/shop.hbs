<h2>Книжный магазин:</h2>
{{#if books.length}}


    <ul class="book-list">
        {{#each books}}
        <li class="book-item">
            
      <div class="card" style="padding: 10px">
        <span class="card-title" style="color: black; margin: 0 auto">{{author}}</span>
        <div class="card-image" style="background: url({{image}}) 100% 100% no-repeat"></div>
        <h6 style="font-weight: bold; margin: 0 auto; text-align: center;">{{name}}</h6>
        <div class="card-content">
          <p style="text-transform: center; text-align: center">Эта книга эксклюзивно доступна только в нашем магазине всего за {{price}} 00 копеек!</p>
        </div>
        <div class="card-action">
          <div class="card-action__first">
            <a class="waves-effect waves-light btn-small" href="/shop/{{id}}">Ознакомиться</a>
            <a class="waves-effect waves-light btn-small" href="/shop/{{id}}/edit?allow=true">Редактировать</a>
          </div>
          <form action="/card/add" method="POST" class="addpost">
            <input type="hidden" name="id" value={{id}}>
            <button class="btn btn-primary" style="background: red" type="submit">КУПИТЬ</button>
          </form>
        </div>
      </div>
    
        </li>
        {{/each}}
    </ul>

  
  {{else}}
<h4>No Books in Library :(((</h4>
  {{/if}}